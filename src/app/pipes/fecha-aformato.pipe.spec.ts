import { FechaAFormatoPipe } from './fecha-aformato.pipe';

describe('FechaAFormatoPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaAFormatoPipe();
    expect(pipe).toBeTruthy();
  });
});
